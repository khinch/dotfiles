[Unit]
Description=WayVNC server
After=graphical-session.target
Wants=graphical-session.target

[Service]
Type=simple
Environment=WAYLAND_DISPLAY=wayland-1
ExecStart=/usr/bin/wayvnc --keyboard=gb localhost
Restart=always
RestartSec=3

[Install]
WantedBy=default.target

